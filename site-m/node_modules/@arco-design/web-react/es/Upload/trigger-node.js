var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import React, { useState, useContext, useRef } from 'react';
import cs from '../_util/classNames';
import Button from '../Button';
import IconUpload from '../../icon/react-icon/IconUpload';
import IconPlus from '../../icon/react-icon/IconPlus';
import { ConfigContext } from '../ConfigProvider';
import { isArray } from '../_util/is';
import { contains } from '../_util/dom';
var isAcceptFile = function (file, accept) {
    if (accept && file) {
        var accepts = isArray(accept)
            ? accept
            : accept
                .split(',')
                .map(function (x) { return x.trim(); })
                .filter(function (x) { return x; });
        var fileExtension_1 = file.name.indexOf('.') > -1 ? file.name.split('.').pop() : '';
        return accepts.some(function (type) {
            var text = type && type.toLowerCase();
            var fileType = (file.type || '').toLowerCase();
            if (text === fileType) {
                // 类似excel文件这种
                // 比如application/vnd.ms-excel和application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
                // 本身就带有.字符的，不能走下面的.jpg等文件扩展名判断处理
                // 所以优先对比input的accept类型和文件对象的type值
                return true;
            }
            if (/\/\*/.test(text)) {
                // image/* 这种通配的形式处理
                return fileType.replace(/\/.*$/, '') === text.replace(/\/.*$/, '');
            }
            if (/\..*/.test(text)) {
                // .jpg 等后缀名
                return text === "." + (fileExtension_1 && fileExtension_1.toLowerCase());
            }
            return false;
        });
    }
    return !!file;
};
var getFiles = function (fileList, accept) {
    if (!fileList) {
        return;
    }
    var files = [].slice.call(fileList);
    if (accept) {
        files = files.filter(function (file) {
            return isAcceptFile(file, accept);
        });
    }
    return files;
};
var loopDirectory = function (items, accept, callback) {
    var _loopDirectory = function (item) {
        if (item.isFile) {
            item.file(function (file) {
                if (isAcceptFile(file, accept)) {
                    Object.defineProperty(file, 'webkitRelativePath', {
                        value: item.fullPath.replace(/^\//, ''),
                    });
                    callback(file);
                }
            });
        }
        else if (item.isDirectory) {
            // item 是个文件夹
            var reader = item.createReader();
            reader.readEntries(function (entries) {
                entries.forEach(_loopDirectory);
            });
        }
    };
    [].slice
        .call(items)
        .forEach(function (item) { return item.webkitGetAsEntry && _loopDirectory(item.webkitGetAsEntry()); });
};
var TriggerNode = function (props) {
    var _a, _b;
    var locale = useContext(ConfigContext).locale;
    var _c = __read(useState(false), 2), isDraging = _c[0], setIsDraging = _c[1];
    var nodeRef = useRef();
    var tip = props.tip, children = props.children, disabled = props.disabled, drag = props.drag, listType = props.listType, prefixCls = props.prefixCls, accept = props.accept, multiple = props.multiple;
    var nodeProps = {
        disabled: disabled,
    };
    var onDragOver = function (e) {
        e.preventDefault();
        if (!disabled && !isDraging) {
            setIsDraging(true);
        }
    };
    if (children === null) {
        return null;
    }
    return (React.createElement("div", { ref: nodeRef, className: prefixCls + "-trigger", onClick: disabled ? undefined : props.onClick, onDrop: function (e) {
            e.preventDefault();
            if (!disabled) {
                setIsDraging(false);
                if (props.directory) {
                    loopDirectory(e.dataTransfer.items, accept, function (file) {
                        props.onDragFiles && props.onDragFiles([file]);
                    });
                }
                else {
                    var files = getFiles(e.dataTransfer.files, accept);
                    props.onDragFiles && props.onDragFiles(multiple ? files : files.slice(0, 1));
                }
            }
        }, onDragLeave: function (e) {
            e.preventDefault();
            if (!nodeRef.current || !contains(nodeRef.current, e.target)) {
                setIsDraging(false);
            }
        }, onDragOver: onDragOver }, React.isValidElement(children) ? (React.createElement("div", { className: cs((_a = {}, _a[prefixCls + "-trigger-custom-active"] = isDraging, _a)) }, React.cloneElement(children, nodeProps))) : listType === 'picture-card' ? (React.createElement("div", { className: prefixCls + "-trigger-picture-wrapper" },
        React.createElement("div", { className: prefixCls + "-trigger-picture" },
            React.createElement("div", { className: prefixCls + "-trigger-picture-text" },
                React.createElement(IconPlus, null))),
        tip && React.createElement("div", { className: prefixCls + "-trigger-tip" }, tip))) : drag ? (React.createElement("div", { draggable: false, className: cs(prefixCls + "-trigger-drag", (_b = {},
            _b[prefixCls + "-trigger-drag-active"] = isDraging,
            _b)) },
        React.createElement(IconPlus, null),
        React.createElement("p", { className: prefixCls + "-trigger-drag-text" }, isDraging ? locale.Upload.dragHover : locale.Upload.drag),
        tip && React.createElement("div", { className: prefixCls + "-trigger-tip" }, tip))) : (React.createElement(Button, __assign({}, nodeProps, { type: "primary", className: prefixCls + "-trigger-with-icon" }),
        React.createElement(IconUpload, null),
        locale.Upload.upload))));
};
export default TriggerNode;
