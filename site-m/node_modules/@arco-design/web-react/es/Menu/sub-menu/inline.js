var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import React, { useContext, useEffect } from 'react';
import { CSSTransition } from 'react-transition-group';
import cs from '../../_util/classNames';
import useStateWithPromise from '../../_util/hooks/useStateWithPromise';
import IconDown from '../../../icon/react-icon/IconDown';
import { processChildren, isChildrenSelected } from '../util';
import MenuContext from '../context';
import MenuIndent from '../indent';
import { useHotkeyHandler } from '../hotkey';
var SubMenuInline = function (props) {
    var _a;
    var _key = props._key, children = props.children, style = props.style, className = props.className, title = props.title, level = props.level, forwardedRef = props.forwardedRef, rest = __rest(props, ["_key", "children", "style", "className", "title", "level", "forwardedRef"]);
    var _b = useContext(MenuContext), prefixCls = _b.prefixCls, levelIndent = _b.levelIndent, _c = _b.openKeys, openKeys = _c === void 0 ? [] : _c, _d = _b.selectedKeys, selectedKeys = _d === void 0 ? [] : _d, icons = _b.icons, onClickSubMenu = _b.onClickSubMenu, collectInlineMenuKeys = _b.collectInlineMenuKeys;
    var baseClassName = prefixCls + "-inline";
    var isOpen = (openKeys === null || openKeys === void 0 ? void 0 : openKeys.indexOf(_key)) > -1;
    var _e = __read(useStateWithPromise(isOpen ? 'auto' : 0), 2), height = _e[0], setHeight = _e[1];
    var subMenuClickHandler = function () { return onClickSubMenu(_key, level, 'inline'); };
    var isActive = useHotkeyHandler(_key, function (isActive, type) { return isActive && type === 'enter' && subMenuClickHandler(); });
    useEffect(function () {
        collectInlineMenuKeys(props._key);
        return function () {
            collectInlineMenuKeys(props._key, true);
        };
    }, []);
    // 注意：此处 rest 中的属性会被 cloneElement 传递给 Menu.Item
    // 当 SubMenuInline 增加了新的属性时，需要将其从 Menu.item 传递给 div 的属性中 omit
    var childrenList = processChildren(children, __assign(__assign({}, rest), { level: level + 1 }));
    // 根据level偏移
    var header = (React.createElement("div", { className: cs(baseClassName + "-header", (_a = {},
            _a[prefixCls + "-active"] = isActive,
            _a[prefixCls + "-selected"] = isChildrenSelected(children, selectedKeys),
            _a)), onClick: function () { return subMenuClickHandler(); } },
        React.createElement(MenuIndent, { level: level, prefixCls: prefixCls, levelIndent: levelIndent }),
        React.createElement("span", null, title),
        React.createElement("span", { className: prefixCls + "-icon-suffix " + (isOpen ? 'is-open' : '') }, icons && icons.horizontalArrowDown ? icons.horizontalArrowDown : React.createElement(IconDown, null))));
    var content = (React.createElement("div", { className: cs(baseClassName + "-content"), style: { height: height } }, childrenList));
    return (React.createElement("div", { ref: forwardedRef, className: cs(baseClassName, className), style: style },
        header,
        React.createElement(CSSTransition, { in: isOpen, timeout: 200, classNames: baseClassName, unmountOnExit: false, onEnter: function (element) {
                setHeight(0).then(function () {
                    setHeight(element.scrollHeight);
                });
            }, onEntered: function () {
                setHeight('auto');
            }, onExit: function (element) {
                setHeight(element.scrollHeight).then(function () {
                    setHeight(0);
                });
            } }, content)));
};
export default SubMenuInline;
