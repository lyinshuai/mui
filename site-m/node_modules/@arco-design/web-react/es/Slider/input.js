var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import React, { memo } from 'react';
import cs from '../_util/classNames';
import InputNumber from '../InputNumber';
var Input = function (props) {
    var _a;
    var value = props.value, range = props.range, min = props.min, max = props.max, step = props.step, disabled = props.disabled, prefixCls = props.prefixCls, onChange = props.onChange;
    var inputProps = {
        min: min,
        max: max,
        step: step,
        disabled: disabled,
        hideControl: true,
    };
    var sortValue = __spreadArray([], __read(value), false).sort(function (a, b) { return a - b; });
    function handleChange(val) {
        onChange(val);
    }
    return (React.createElement("div", { className: cs(prefixCls + "-input", (_a = {}, _a[prefixCls + "-input-group"] = range, _a)) },
        range && [
            React.createElement(InputNumber, __assign({ value: sortValue[0], key: 0 }, inputProps, { onChange: function (val) {
                    handleChange([val, sortValue[1]]);
                } })),
            React.createElement("div", { key: 1, className: prefixCls + "-input-range" },
                React.createElement("span", { className: prefixCls + "-input-range-content" })),
        ],
        React.createElement(InputNumber, __assign({ key: 2, value: sortValue[1] }, inputProps, { onChange: function (val) {
                handleChange([sortValue[0], val]);
            } }))));
};
export default memo(Input);
