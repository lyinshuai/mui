import { UploadListProps } from '../interface';
export declare const FileList: {
    (props: UploadListProps): JSX.Element;
    defaultProps: {
        listType: string;
    };
    displayName: string;
};
export default FileList;
