// @ts-nocheck
import React, { useEffect, useRef, useState } from "react";
import Spec from "./spec";
import Development from "./development";
import Ecosystem from "./ecosystem";
import About from "./about";
import styles from "./style/index.module.less";
function NavDropdown(_a) {
    var lang = _a.lang, activeModule = _a.activeModule;
    var _b = useState(0), height = _b[0], setHeight = _b[1];
    var dropdownRef = useRef(null);
    useEffect(function () {
        if (activeModule) {
            var height1 = dropdownRef.current.childNodes[0]
                ? dropdownRef.current.childNodes[0].clientHeight
                : 0;
            setHeight(height1);
        }
    }, [activeModule]);
    return (React.createElement("div", { className: styles.dropdown, style: { height: height }, ref: dropdownRef },
        activeModule === "spec" && React.createElement(Spec, { lang: lang }),
        activeModule === "development" && React.createElement(Development, { lang: lang }),
        activeModule === "ecosystem" && React.createElement(Ecosystem, { lang: lang }),
        activeModule === "about" && React.createElement(About, { lang: lang })));
}
export default NavDropdown;
