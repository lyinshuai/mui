export var internalUrl = "https://arco.bytedance.net";
export var externalUrl = "https://arco.design";
export var hostname = location.hostname !== "arco.design" &&
    location.hostname !== "arco.bytedance.net"
    ? "arco.design"
    : location.hostname;
export var isExternal = hostname === "arco.design";
export var isInternal = hostname === "arco.bytedance.net";
export var apiBasename = "https://".concat(hostname || "arco.design");
export var getLoginHref = function () {
    // let host = "sso.meetsocial.cn";
    // const isProd = location.host === "m.meetsocial.cn";
    // return `https://${isProd ? "test-" : "dev-"}${host}?redirect=${encodeURIComponent(
    //   location.href
    // )}`;
    return "/login?redirect=".concat(encodeURIComponent(location.href));
};
